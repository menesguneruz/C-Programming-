#include <stdio.h>

int main( void )
{
	/*
	int array[] = { 1, 2, 3, 4, 5 };
	
	printf( "%d", array[2] );
	*/
	
	/*
	int array[] = { 1, 2, 3, 4, 5 };
	int i = 0;
	
	for( ; i <= 4 ; i++ ){
		
		printf( "%d\n", array[i] );
		
	}
	*/
	
	/*
	float toplam = 0.0;
	float sayilar[5];
	int i;
	
	for( i = 0 ; i < 5 ; i++ ) {
		printf( "Bir sayi giriniz:" );
		scanf( "%f", &sayilar[i] );
	}
	
	for( i = 0 ; i < 5 ; i ++ ){
		toplam += sayilar[i];
	}
	
	printf( "Girilen sayilarin aritmetik ortalamasi %.2f dir", toplam / 5.0 );
	*/
	
	
	/*
	char name[] = "Mustafa";
	int i;
	
	for( i = 0 ; i <= 6 ; i++ ){
		
		printf( "%c", name[i] );
	}
	*/
	
	return 0;
}
