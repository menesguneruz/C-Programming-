
#include<stdio.h>

int main(void)
{
    /*	
	int x = 5; 
	 printf("%d",x);
	 */
	
	/*
	float f = 3.1;
	double d = 4.21; 
	printf("%f %f", f,d); 
	*/
	
	/*
	char c1 = 'c'; 
	printf("%c",c1);
	*/
	
	/*
	printf("%s","C Programlama");
	*/
	
	/*
	int a1 = 42;
	char b1 = 66; 
	char b2 = 'B';
	float c1 = 4.2;
	double d1 = 0.21;
	printf("%d %c %d %.1f %f %s\n",a1,b1,b2,c1,d1,"Selam");
	*/
	
	/*
	printf("%d byte\n",sizeof(int));
	printf("%d byte\n",sizeof(char));
	printf("%d byte\n",sizeof(short));
	printf("%d byte\n",sizeof(long int));
	printf("%d byte\n",sizeof(float));
	printf("%d byte\n",sizeof(double));
	*/


/*	
//A program adding two integers.
#include <stdio.h>

int main( void )
{
	int a,b,c;
	
	printf( "Enter the first value:" );
	scanf( "%d", &a );
	
	printf( "Enter the second value:" );
	scanf( "%d", &b );
	
	c = a + b;
	
	printf( "The sum of two integers:%d", c );
	scanf( "%d + %d = %d", a, b, c );
	
	return 0;
}
*/


/*
//Adding a few integers.
#include <stdio.h>

int main( void )
{
	int number;
	int sumOfNumbers = 0;
	int counter = 1;
	
	while( counter <= 15 )
	{
		printf( "Enter number %d:", counter );
		scanf( "%d", &number );
		
		sumOfNumbers = sumOfNumbers + number;
		counter = counter + 1;
	}
	
	printf( "The sum of numbers is %d", sumOfNumbers );
	return 0;
}
*/

/*
//Adding odd integers.
#include <stdio.h>

int main( void )
{
	int number;
	int sumOfNumbers = 0;
	int counter = 1;
	
	while( counter <= 10 )
	{
		printf( "Enter odd number %d:", counter );
		scanf( "%d", &number );
		
		if( number %2 == 1 )
		{
			sumOfNumbers = sumOfNumbers + number;
			counter = counter + 1;
			
		}
		else{
			sumOfNumbers = sumOfNumbers;
			counter = counter + 1;
		}
		
	}
	printf( "\nSum of the odd numbers is %d", sumOfNumbers );
	*/
	return 0;
}

/*
#include <stdio.h>

int main( void )
{
	int number;
	int square;
	
	printf( "Enter a number:" );
	scanf( "%d", number );
	
	
	printf( "Square of the number is %d", square );
	
	return 0;
}
*/
